import React, { Component } from 'react';
import {
  AppRegistry,
  StyleSheet,
  Text,
  View
} from 'react-native';

import Router from '../../containers/Router';

class App extends Component {
  render() {
    return (
      <Router/>
    );
  }
}

export default App;
